node_modules/
dist/
dist-web/
coverage/
.DS_Store
*.log
.env
.env.*
!apps/web/.env.production
# Private legacy documentation retained only for local compatibility archaeology.
.legacy-inspect/
# Never commit supplied TabWin binaries or private/raw fixture data by default.
legacy-binaries/
private-fixtures/
dist-web-old/
# Playwright run output. The report is a CI artifact, not a tracked file.
test-results/
.cache/
playwright-report/
blob-report/
playwright/.cache/

# Pacotes de handoff nunca entram no repositório.
*.zip

# Saída do gerador diferencial por seed (regenerável a partir da seed)
.seed-cases/

# Arquivos do DATASUS usados para gerar as capturas do manual.
# São públicos e re-baixáveis; não precisam pesar no repositório.
e2e/fixtures/manual/
