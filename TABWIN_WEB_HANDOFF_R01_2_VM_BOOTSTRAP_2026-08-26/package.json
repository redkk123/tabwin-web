{
  "name": "tabwin-web",
  "version": "0.1.1-dev",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run test"
  },
  "dependencies": {
    "@precisa-saude/datasus-dbc": "2.0.2"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
