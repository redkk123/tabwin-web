# G001 TabWin 4.15 reference capture

The user executed the protocol in the isolated TabWin 4.15 directory and
provided `result.xls`, a 415-byte BIFF export generated directly by TabWin.
The workbook itself preserves:

- the table title `Movimento de AIH - Arquivos Reduzidos`;
- the analysis title `Freqüência segundo Complexidade do Procedimento`;
- the row header and exact legacy measure spelling;
- four ordered category rows and their numeric values;
- total `4315`.

No row, label or value was edited in Excel. A separate TabWin log and screenshot
were not supplied; they are therefore recorded as unavailable rather than
fabricated. The lossless workbook is the numerical and structural oracle.
